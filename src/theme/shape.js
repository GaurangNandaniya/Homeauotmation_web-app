// src/theme/shape.js
// MUI default base of 4. Custom components scale this via the `borderRadius`
// shorthand in sx (e.g. `borderRadius: 4` → 16px tile radius).
const shape = {
  borderRadius: 4,
};

export default shape;
